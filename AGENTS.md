# 项目协作规则

## 语言与说明

- 始终使用中文说明用户操作，命令、路径和代码识别符保持原样。
- 面向用户和 Agent 的说明默认使用中文。
- 不假设用户具备 GitHub、终端、Python、Node.js 或系统环境配置经验。

## Skill 真源

- 本仓库所有 Skill 的唯一真源位于 `skills/<skill-name>/`。
- Skill 名称、目录名、frontmatter 的 `name` 和调用名称必须一致。
- 不直接在 `~/.agents/skills/`、`~/.claude/skills/`、`~/.codex/skills/` 或其他 Agent 安装目录中维护长期逻辑。
- 全局 Agent 目录只作为安装结果或 bridge，不作为开发真源。
- 修改或新增文件前先查看 `SOURCE_OF_TRUTH.md`。

## 全局安装

- 本仓库公开 Skill 默认面向全局安装，不以项目级安装作为主要发布方式。
- 全部 Skill 的统一安装命令为 `npx -y skills add CantonFocus/fcskills -g --all`。
- `-g` 只表示安装范围；Skill 真源仍然保存在本仓库。
- 新增 Skill 后，必须验证整包安装和单个 Skill 发现能力。
- 不得在公开安装说明中写入作者电脑的绝对路径。

## Windows 与 macOS 默认兼容

- 所有新 Skill 默认同时支持 Windows 和 macOS。
- 仅包含文字规则的 Skill，也必须检查路径、命令和示例是否跨平台。
- 包含脚本、二进制程序或第三方依赖的 Skill，开发前必须读取 `docs/skill-development.md`。
- 使用系统能力前必须检测操作系统和 CPU 架构。
- 不得无替代方案地依赖 `qlmanage`、`osascript`、AppleScript、`.app` 路径、Windows 注册表或其他单平台功能。
- 不得固定下载某一个操作系统或 CPU 架构的安装包。
- 不得假设 Windows 可执行文件没有 `.exe` 后缀。
- 不得依赖 Bash 通配符、`make`、`chmod`、`curl` 或 POSIX 路径才能运行。
- 平台专属能力必须封装在适配层中，并为另一平台提供对应实现或公共替代方案。
- 缓存、临时文件和配置目录必须使用操作系统标准位置。
- 所有用户路径必须通过跨平台路径库处理。

## 兼容性声明

- 「可以安装」和「可以使用」必须分开验证。
- 未完成 Windows 与 macOS 运行测试时，不得宣称完整支持。
- 如果某个平台暂时无法支持，必须在 `SKILL.md` 和 `README.md` 中明确标注，不得静默失败。
- 平台支持状态以实际代码、自动测试和真实运行结果为准。

## 安全机制

- 正式重命名素材前必须完成预演并取得用户明确同意。
- 网络下载、安装依赖、编译程序和修改系统环境前，必须说明影响并取得授权。
- 不在公开文件中写入本机绝对路径、账号、密钥、素材内容或客户信息。
- 不提交模型、二进制程序、缓存、临时输出和构建产物。

## 发布门槛

每次公开版变更后必须完成：

1. Skill 结构校验。
2. Windows 自动测试。
3. macOS 自动测试。
4. 全局安装发现测试。
5. 无网络的 `preflight` 测试。
6. `--dry-run` 安全测试。
7. 失败回滚测试。
8. 私密信息扫描。
9. `README.md` 与实际支持状态一致性检查。

任一必需测试没有通过，不得将该版本标记为 Windows 与 macOS 完整支持。
